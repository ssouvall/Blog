@using X.PagedList
@using X.PagedList.Mvc.Core
@using X.PagedList.Web.Common
@using RockwellBlog.Services
@inject IBlogImageService _blogImageService
@model IPagedList<Blog>
@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center mb-5">
    <h3 class="mb-3">FEATURED POSTS</h3>
</div>

@foreach(var blog in Model)
            {
                foreach(var post in blog.Posts.Where(p => p.IsFeatured))
                {
                    <div class="row shadow border">
                        <div class="col-md-3">
                            <img src="@_blogImageService.DecodeImage(post.ImageData, post.ContentType)" class="img-fluid" alt="featured post image" />
                        </div>
                        <div class="col-md-9">
                            <h3>@post.Title</h3>
                            <p>@Html.Raw(post.Content)</p>
                        </div>
                    </div>
                    
                }
            }

<hr />

<div class="text-center mb-5">
    <h3 class="mb-3">CATEGORIES</h3>
    @if (User.Identity.IsAuthenticated)
    {
        <a class="btn btn-primary text-decoration-none" asp-controller="Blogs" asp-action="Create">Create New <i class="fas fa-plus"></i></a>
    }
</div>
<div class="row">
    <!--Model is the IEnumerable and blog is the instance of Model-->
    @foreach (var blog in Model)
    {
        @*<div class="post-preview col-lg-4 col-md-6 col-sm-12">*@
        <div class="post-preview col-lg-4 col-md-6 col-sm-12">
            <div class="card mb-5 shadow">
                <img class="post-img img-fluid" src="@_blogImageService.DecodeImage(blog.ImageData, blog.ContentType)" alt="post image" />
                <div class="card-body">
                    <a asp-controller="Posts" asp-action="BlogPostIndex" asp-route-id="@blog.Id">
                        <h3 class="card-title">@blog.Name</h3>
                        <p class="post-subtitle card-text">@blog.Description</p>
                        <p class="post-subtitle card-text">Number of Posts: @blog.Posts.Count</p>
                        <p class="post-meta card-text">Posted by Stephen Souvall on @blog.Created.ToString("MMM dd, yyyy")</p>
                    </a>
                    <div class="row">
                        <div class="col">
                            <a class="btn btn-primary btn-block text-center mt-5 d-flex align-items-center justify-content-center" asp-controller="Posts" asp-action="BlogPostIndex" asp-route-id="@blog.Id">View Blog</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <hr />
    }
</div>

<div class="row">
    <div class="col h3">
        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    </div>
</div>

<div class="row">
    <div class="col">
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }),
           new PagedListRenderOptions
           {
               LiElementClasses = new string[] { "page-item" },
               PageClasses = new string[] { "page-link" }
           }

      )
    </div>
</div>